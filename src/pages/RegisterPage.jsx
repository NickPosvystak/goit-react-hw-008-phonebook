import React from 'react';
import { Outlet } from 'react-router-dom';

const RegisterPage = () => {
  return (
    <div>
      RegisterPage
      <Outlet />
    </div>
  );
};

export default RegisterPage;
